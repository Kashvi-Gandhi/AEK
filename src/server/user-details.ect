<<
@configure
  connected: true
  auth: true
  scripts:[
    "screen"
  ]
>>

<< extend "templates/master.ect" >>

<< content "body" >>

  <% utils = @aek.extension("CMAuthUtilities") %>
  <% attrs = utils.getTokenAttributes() if utils %>

  <script>
    window.userDetails = <%- JSON.stringify(attrs || {}) %>;
    console.log("Raw CMAuth Attributes:", window.userDetails);
  </script>

<< end >>